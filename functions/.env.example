# Agora (for video call token via getAgoraToken callable). Get from https://console.agora.io
# Copy to .env and fill in so "Join call" works on the deployed site (notusapp.com).
AGORA_APP_ID=
AGORA_APP_CERTIFICATE=
